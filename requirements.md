# MaduReader 需求文档

## 项目概述

MaduReader 是一款基于 Tauri 2.x 的跨平台 Markdown 阅读器，采用极简现代风格设计，支持丰富的文档格式化、图表渲染、代码高亮等功能。

## 界面设计

### 单窗口布局

- **主窗口**：单窗口设计，极简风格
- **左侧导航栏**：显示当前目录的 .md 文件列表
  - 可折叠/展开
  - 显示文件树结构
  - 支持快速切换文档
- **主内容区**：Markdown 渲染预览

### 导航栏控制

- 导航栏左侧有控制按钮区域
- **折叠/展开按钮**：点击切换导航栏显示
  - **展开状态**：显示完整侧边栏（目录树/页签列表）
  - **折叠状态**：仅显示图标条（类似 VSCode 侧边栏）
  - 图标条宽度约 48px
  - 点击图标可快速切换对应视图
- **设置按钮**：点击弹出设置子窗口
- **侧边栏位置切换**：可在左侧/右侧切换

### 侧边栏类型

1. **目录树侧边栏**：显示当前打开目录的文件结构
2. **页签侧边栏**：显示已打开的文档页签
   - 支持多页签
   - 双击页签重命名（可选）
   - 中键点击关闭页签
   - 拖拽页签重新排序

### 侧边栏折叠模式（图标条）

折叠后显示图标：

| 图标 | 功能 |
|------|------|
| 📁 | 切换目录树视图 |
| 📄 | 切换页签视图 |
| ⚙️ | 打开设置 |
| 🌙/☀️ | 切换主题 |
| ➕ | 新建页签 |
| 🔄 | 刷新文件树 |

### 设置子窗口

独立的设置窗口，包含所有配置选项：

#### 通用设置
- 主题模式（亮色/暗色）
- 字体大小
- 默认缩放比例
- 启动时行为

#### 外观设置
- 预设主题选择
- 自定义 CSS 导入/导出
- 代码高亮主题
- 行号显示

#### 编辑器设置
- 自动保存
- 标签页宽度
- 显示行尾符号

#### 阅读设置
- 默认缩放比例
- 行间距
- 段落间距

#### 高级设置
- Mermaid 配置
- PlantUML 服务器地址
- KaTeX 配置
- 代理设置

### 主题模式

- **亮色模式（Light Mode）**：默认主题
- **暗色模式（Dark Mode）**：深色主题
- 支持系统主题自动跟随
- 可在设置中手动切换
- 主题切换即时生效

## 核心功能

### 1. CSS 样式渲染 / 美化主题 ✅

**需求描述**
- 支持自定义 CSS 主题
- 提供多种预设主题（亮色/暗色）
- 支持用户自定义样式表

**技术要点**
- 主题切换应即时生效
- 支持导入/导出主题配置

---

### 2. Mermaid 图表支持 ✅

**需求描述**
- 支持渲染 Mermaid 语法图表
- 支持的图表类型：
  - 流程图 (flowchart)
  - 时序图 (sequenceDiagram)
  - 类图 (classDiagram)
  - 状态图 (stateDiagram)
  - 甘特图 (gantt)
  - 饼图 (pie)
  - 用户旅程图 (journey)
  - C4 架构图 (C4)

**技术要点**
- 使用 Mermaid.js 库
- 支持图表缩放
- 支持图表导出

---

### 3. PlantUML 图表支持 ✅

**需求描述**
- 支持渲染 PlantUML 语法图表
- 支持的图表类型：
  - 用例图 (usecase)
  - 类图 (class)
  - 时序图 (sequence)
  - 活动图 (activity)
  - 状态图 (state)
  - 组件图 (component)
  - 部署图 (deployment)

**技术要点**
- 使用 PlantUML Server 或本地渲染
- 支持 SVG 输出
- 支持图表导出

---

### 4. Markdown PPT 模式（--- 分隔）✅

**需求描述**
- 使用 `---` 分隔符将 Markdown 分割为多页幻灯片
- 支持幻灯片切换
- 支持演示模式（全屏）

**功能要点**
- 键盘快捷键导航（方向键、空格）
- 鼠标滚轮导航
- 进度条显示
- 幻灯片缩略图预览

---

### 5. 缩放功能（50% - 200%）✅

**需求描述**
- 支持文档缩放显示
- 缩放范围：50% - 200%
- 默认缩放：100%

**功能要点**
- 快捷键支持：Ctrl+/Ctrl- 调整缩放
- 显示当前缩放百分比
- 缩放设置持久化

---

### 6. 导出 HTML ✅

**需求描述**
- 将 Markdown 文档导出为独立 HTML 文件
- 导出文件包含完整样式和资源
- 支持自定义导出选项

**功能要点**
- 选择导出范围（当前文档/全部）
- 选择主题样式
- 包含图片资源
- 支持批量导出

---

### 7. 导出 PDF ✅

**需求描述**
- 将 Markdown 文档导出为 PDF 文件
- 保持文档格式和样式
- 支持分页设置

**功能要点**
- 选择导出范围
- 自定义页面设置（A4/A3、方向）
- 选择主题样式
- 支持批量导出

---

### 8. 代码语法高亮（20+ 语言）✅

**需求描述**
- 支持 20+ 编程语言的语法高亮
- 支持的语言包括：
  - Python, JavaScript, TypeScript, Java, C/C++
  - Go, Rust, C#, Ruby, PHP
  - SQL, Shell, PowerShell, Lua
  - HTML, CSS, SCSS, JSON, YAML
  - Markdown, XML, Proto
  - Kotlin, Swift, R, MATLAB

**功能要点**
- 使用 highlight.js 库
- 支持行号显示
- 支持代码复制
- 支持主题切换

---

### 9. 本地/远程图片自动加载 ✅

**需求描述**
- 自动加载 Markdown 中引用的图片
- 支持本地图片路径
- 支持远程图片 URL

**功能要点**
- 本地图片：相对路径、绝对路径
- 远程图片：HTTP/HTTPS URL
- 图片缓存机制
- 图片加载失败时显示占位符
- 支持图片预览和下载

---

### 10. 页面内查找 ✅

**需求描述**
- 支持在文档内查找文本
- 显示匹配结果数量
- 支持高亮显示匹配项

**功能要点**
- 快捷键：Ctrl+F 打开查找框
- 支持大小写敏感切换
- 支持正则表达式
- 上一个/下一个导航
- 跳转到匹配位置

---

## 扩展功能

### 11. 复杂数学公式（LaTeX / KaTeX）✅

**需求描述**
- 支持渲染 LaTeX 数学公式
- 使用 KaTeX 库

**功能要点**
- 行内公式：`$...$` 或 `\(...\)`
- 块级公式：`$$...$$` 或 `\[...\]`
- 支持公式编号
- 支持公式复制为 LaTeX 代码
- 支持公式缩放

---

### 12. 自定义协议超链接（跳转外部程序）✅

**需求描述**
- 支持自定义协议的超链接
- 点击链接可跳转到外部程序

**功能要点**
- 支持的协议类型：
  - `mailto:` - 邮件客户端
  - `tel:` - 电话拨号
  - `http(s):` - 浏览器
  - 自定义协议（如：`myapp://`）
- 可配置协议处理器
- 安全提示：首次打开外部程序时提示用户确认

---

## 技术栈

| 组件 | 技术选型 |
|------|----------|
| 桌面框架 | Tauri 2.x |
| 前端框架 | Vue 3 + Vite |
| Markdown 解析 | markdown-it |
| 代码高亮 | highlight.js |
| 图表渲染 | Mermaid + PlantUML |
| 数学公式 | KaTeX |

## 目标平台

- **Windows**（优先）
  - 单文件可执行程序（.exe）
  - 配置文件存储：`%APPDATA%\MaduReader\config.json`
  - 用户数据目录：`%APPDATA%\MaduReader\`
  - 支持静默安装/卸载

- **macOS**（后续支持）

## 构建目标

- **单文件可执行程序**：使用 Tauri bundle 生成单个 .exe 文件
- **无需安装**：双击即可运行
- **便携模式**：支持从任意位置运行（可选）
- **自动更新**：支持应用自动更新（可选）

## 设计原则

- **极简主义**：去除冗余元素，聚焦核心功能
- **现代风格**：采用圆角、阴影、流畅动画等现代 UI 元素
- **一致性**：统一的视觉语言和交互模式
- **高效性**：快捷键支持，快速操作

## 文件操作

### 拖拽打开

- **支持拖拽 .md 文件到窗口内**：直接打开文件
- 拖拽时显示视觉反馈（高亮边框/背景）
- 支持同时拖入多个文件（依次打开或添加到导航栏）
- 支持从资源管理器/编辑器拖入

### 命令行支持

```
MaduReader.exe [options] <path>
```

**参数说明**

| 参数 | 说明 |
|------|------|
| `path` | 文件路径或目录路径 |
| `-w, --window` | 独立窗口模式打开 |
| `-t, --tab` | 页签模式打开（默认） |
| `-s, --side [left\|right]` | 侧边栏位置（左侧/右侧） |
| `--help` | 显示帮助信息 |

**使用示例**

```bash
# 打开单个文件（页签模式，默认左侧栏）
MaduReader.exe README.md

# 独立窗口打开
MaduReader.exe -w README.md

# 页签模式，侧边栏在右侧
MaduReader.exe -t --side right README.md

# 打开目录
MaduReader.exe ./docs

# 打开目录，独立窗口
MaduReader.exe -w ./docs
```

### 目录打开

- **传入目录路径时**：
  - 显示目录树在侧边栏
  - 自动打开 `README.md` 或 `INDEX.md`（优先级：README.md > INDEX.md）
  - 如果都没有，打开第一个 .md 文件
  - 如果目录为空，显示空白编辑器

### 其他打开方式

- 菜单栏"打开文件"
- 快捷键 Ctrl+O

## 文档版本

- 版本：1.1
- 创建日期：2026-06-23
- 最后更新：2026-06-23
